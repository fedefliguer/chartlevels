from setuptools import setup
from chartlevels import __version__

setup(
    name='chartlevels',
    version=__version__,

    url='https://github.com/fedefliguer/chartlevels',
    author='Federico Fliguer',
    author_email='federico.fliguer@gmail.com',

    py_modules=['chartlevels'],
)
